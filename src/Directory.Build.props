<Project>

  <!-- Convenciones comunes a todos los proyectos de la solución (AGENTS.md §2). -->
  <PropertyGroup>
    <LangVersion>12</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
